<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-12'>
      <h2>Follow Users</h2>
       <% if @users.present? %>
       <%= render 'users/index',users: @users %>
       <% else %>
       <%= 'ユーザーはいません' %>
       <% end %>
    </div>
  </div>
</div>